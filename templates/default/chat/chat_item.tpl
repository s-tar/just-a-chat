<div class="chat {{'active' if is_active}}" data-name="{{chat.name|e}}" data-id="{{chat.id}}">
    <span class="fa fa-envelope-o message-icon"></span>
    <span class="fa fa-users icon"></span>
    <span class="name">{{chat.name|e}}</span>
    <div class="fa fa-times delete"></div>
</div>